# Runtime state — never version
backups/
cache/
file-history/
paste-cache/
projects/
session-env/
sessions/
shell-snapshots/
tasks/
history.jsonl
stats-cache.json
.last-cleanup
.last-update-result.json

# Local-only overrides
settings.local.json

# Plugin state (rewritten on every session — timestamp churn)
plugins/

.DS_Store
